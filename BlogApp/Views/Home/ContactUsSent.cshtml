@using PagedList
@using PagedList.Mvc
@{
    ViewBag.Title = "ContactUsSent";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Contact Us Messages</h2>

<br />
<br />
<br />
<br />
<table class="table table-hover">
    <thead>
        <tr>

            <th> Name</th>
            <th>  Mobile</th>
            <th>  Subject</th>
            <th> Message Date</th>
            <th></th>
        </tr>
    </thead>
    @foreach (var message in ViewBag.onePageOfcontactUss)
    {
        <tbody class="table-bordered">
            <tr>
                <td> @message.Name</td>
                <td> @message.Mobile</td>
                <td> @message.Subject</td>
                <td> @message.CreatedAt</td>

                <td>
                  
                    @Html.ActionLink("Read", "ContactUsDetail", new { id = message.ContactUsId }, new { @class = "btn btn-info btn-sm" })
                </td>
            </tr>
        </tbody>

    }
</table>
<div class="pagedList">
    @Html.PagedListPager((IPagedList)ViewBag.onePageOfcontactUss, page => Url.Action("ContactUsSent", new { page }))
</div>